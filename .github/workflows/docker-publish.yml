name: Publish Docker Image
on: 
  push:
    branches:
      - master
      - develop

jobs:
  build-push:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - uses: docker/setup-buildx-action@v2
      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: "Build Push Node"
        uses: docker/build-push-action@v3
        with:
          context: node
          push: true
          tags: bjubes/smart-node:${{ github.ref_name }}
        timeout-minutes: 240
      - name: "Build Push Ingest"
        uses: docker/build-push-action@v3
        with:
          context: python/ingest_server
          push: true
          tags: bjubes/smart-ingest:${{ github.ref_name }}
        timeout-minutes: 240
      - name: "Build Push Geocode"
        uses: docker/build-push-action@v3
        with:
          context: python/geocode
          push: true
          tags: bjubes/smart-geocode:${{ github.ref_name }}
        timeout-minutes: 240
      - name: "Build Push dev UI"
        uses: docker/build-push-action@v3
        with:
          context: frontend
          file: Dockerfile.dev
          push: true
          tags: bjubes/smart-ui-dev:${{ github.ref_name }}
        timeout-minutes: 240
      - name: "Build Push prod UI"
        uses: docker/build-push-action@v3
        with:
          context: frontend
          push: true
          tags: bjubes/smart-ui:${{ github.ref_name }}
        timeout-minutes: 240
